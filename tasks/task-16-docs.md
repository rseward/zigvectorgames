# Task 16: Polish and documentation

Write full API docs for vgame, READMEs for each game, verify all builds.

- vgame/README.md: full API reference (every public type + function)
- vgame/docs/api.md: detailed examples and usage patterns
- Each game README: title, features, controls, build/run instructions
- Verify: every project compiles with `zig build`